namespace KBGodotBuilderWizard.Models;

public enum EOperatingSystem
{
    Unknown = 0,
    Windows = 1,
    Linux = 2,
    LinuxServer = 3,
    MacOS = 4,
    Web = 5,
}

public enum EProcessor
{
    X86 = 0,
    X64 = 1,
    ARM = 2,
}